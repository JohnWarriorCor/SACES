import { TestBed } from '@angular/core/testing';

import { Tabla2Condicion7Service } from './tabla2-condicion7.service';

describe('Tabla2Condicion7Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: Tabla2Condicion7Service = TestBed.get(Tabla2Condicion7Service);
    expect(service).toBeTruthy();
  });
});
