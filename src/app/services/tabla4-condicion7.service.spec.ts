import { TestBed } from '@angular/core/testing';

import { Tabla4Condicion7Service } from './tabla4-condicion7.service';

describe('Tabla4Condicion7Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: Tabla4Condicion7Service = TestBed.get(Tabla4Condicion7Service);
    expect(service).toBeTruthy();
  });
});
