import { TestBed } from '@angular/core/testing';

import { Tabla1Condicion7Service } from './tabla1-condicion7.service';

describe('Tabla1Condicion7Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: Tabla1Condicion7Service = TestBed.get(Tabla1Condicion7Service);
    expect(service).toBeTruthy();
  });
});
