import { TestBed } from '@angular/core/testing';

import { Tabla5Condicion7Service } from './tabla5-condicion7.service';

describe('Tabla5Condicion7Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: Tabla5Condicion7Service = TestBed.get(Tabla5Condicion7Service);
    expect(service).toBeTruthy();
  });
});
